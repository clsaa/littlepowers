interface:
  display_name: "Executing Plans"
  short_description: "Execute, checkpoint, and verify an approved plan"
  default_prompt: "Use $executing-plans to implement and verify this approved plan."
